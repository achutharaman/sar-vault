# UI layer

Angular standalone components with signals. Reads the vault domain model, never
touches crypto or storage directly (enforced by ESLint). Contains no security logic
beyond input handling and display.
